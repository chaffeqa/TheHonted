<h1>Listing text_contents</h1>

<table>
  <tr>
    <th>Text content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @text_contents.each do |text_content| %>
  <tr>
    <td><%= text_content.text_content %></td>
    <td><%= link_to 'Show', text_content %></td>
    <td><%= link_to 'Edit', edit_text_content_path(text_content) %></td>
    <td><%= link_to 'Destroy', text_content, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Text content', new_text_content_path %>
